/**
 * Podstawcie pod zmienne pierwszą i ostatnią wartość następującej tablicy: [ "snow", "rain", "sun" ].
 * Wypiszcie zmienne w konsoli.
 */

let [param1, ,param2] = ["snow","rain","sun"];

console.log(param1);
console.log(param2);